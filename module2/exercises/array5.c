#include <stdio.h>

void compute(int a[5]);

int main (void)
{
    int sample[] = {
    compute sample;
    }

void commpute(int a)
{
    int max = a[0];
    int min = a[0];
    int sum = 0;

    for (int i = 0; i < 5; i++)
    {
        if (a[i] > max)
        {
            max = a[i]
        }

        if (a[i] < min)
        {
            min = a[i]
        }

        sum = sum + a[i]
    }

    float avg = (float) sum / 5;
    printf("max is %i\n", max);
    printf("min is %i\n", min);
    printf("avg is %i\n", avg);
}
}

/* INSTRUCTIONS: Write a function that takes in an array containing
five integers as an argument, and prints out the following values:
    The largest value in the array
    The smallest value in the array
    The average of the values in the array
The end! */
