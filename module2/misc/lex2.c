#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // grab the age of students
    int n - 4;
    int age[n];

    // DO STUFF
    for (int i = 0; i < n; i++)
    {
        // age[n] - GetInt();
    }
}
