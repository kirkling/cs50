#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    do
    {
        printf("O hai! How much change is owed? ")
        float return = GetFloat();
    }
    while(return < 0);

    int amount = (return *= 100.00)

    while (amount >= 25)
    {
        
    }
}
